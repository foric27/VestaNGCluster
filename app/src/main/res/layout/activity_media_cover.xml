<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/media_cover_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#050505">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <!-- Album Cover -->
        <FrameLayout
            android:layout_width="125dp"
            android:layout_height="125dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="12dp">

            <ImageView
                android:id="@+id/cover_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_media_cover_art"
                android:clipToOutline="true"
                android:contentDescription="@string/media_cover_desc"
                android:scaleType="centerCrop"
                tools:src="@android:drawable/ic_media_play" />

        </FrameLayout>

        <!-- Track Title (carousel scroll with fade edges) -->
        <HorizontalScrollView
            android:id="@+id/title_scroll"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:scrollbars="none"
            android:overScrollMode="never"
            android:fadingEdge="horizontal"
            android:requiresFadingEdge="horizontal"
            android:fadingEdgeLength="24dp">

            <TextView
                android:id="@+id/track_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="none"
                android:singleLine="true"
                android:textColor="@color/oem_cluster_title"
                android:textSize="48sp"
                android:textStyle="bold"
                tools:text="Deep End" />

        </HorizontalScrollView>

        <!-- Track Artist -->
        <TextView
            android:id="@+id/track_artist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:ellipsize="middle"
            android:gravity="center"
            android:singleLine="true"
            android:textColor="@color/oem_cluster_subtitle"
            android:textSize="24sp"
            tools:text="Fousheé" />

        <!-- Source Label -->
        <TextView
            android:id="@+id/media_source_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:text="@string/stream_mode_med"
            android:textColor="@color/oem_cluster_subtitle"
            android:textSize="12sp"
            android:textStyle="bold"
            tools:text="Apple CarPlay" />

        <!-- Playback Progress -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/playback_position"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:gravity="center"
                android:minWidth="30dp"
                android:textColor="@color/oem_cluster_title"
                android:textSize="14sp"
                tools:text="2:00" />

            <ProgressBar
                android:id="@+id/playback_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="0dp"
                android:layout_height="4dp"
                android:layout_weight="1"
                android:indeterminate="false"
                android:max="1000"
                android:progress="0"
                android:progressDrawable="@drawable/seekbar_cluster_progress"
                android:progressTint="@color/oem_cluster_accent"
                android:progressBackgroundTint="@color/oem_cluster_seekbar_bg" />

            <TextView
                android:id="@+id/playback_duration"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:gravity="center"
                android:minWidth="30dp"
                android:textColor="@color/oem_cluster_title"
                android:textSize="14sp"
                tools:text="5:24" />

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
