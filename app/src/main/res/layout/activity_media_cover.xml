<?xml version="1.0" encoding="utf-8"?>
<!--
    MED-экран: вертикальная компоновка по требованию пользователя.
    Порядок сверху вниз:
      1. Source label ("YouTube", 12sp)
      2. Обложка альбома (96dp, по центру)
      3. Название трека (14sp, по центру)
      4. Исполнитель (12sp, по центру)
      5. Progress bar (4dp)
    Фон — @color/oem_cluster_bg (чёрный).
    Обложка/текст не пересекаются, layout — вертикальный стек.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/media_cover_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/oem_cluster_bg">

    <!--
        Корень — чёрный opaque фон, полностью закрывает навигатор.
        media_visible_area позиционируется через код в configureMediaVisibleArea().
    -->
    <LinearLayout
        android:id="@+id/media_visible_area"
        android:layout_width="380dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@color/oem_cluster_bg"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingTop="8dp"
        android:paddingEnd="12dp"
        android:paddingBottom="8dp">

        <!-- 1. Source Label: "YouTube" / "Apple CarPlay" и т.п. -->
        <TextView
            android:id="@+id/media_source_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="@color/oem_cluster_subtitle"
            android:textSize="12sp"
            tools:text="YouTube" />

        <!-- 2. Обложка альбома: 96dp, по центру, под source label -->
        <ImageView
            android:id="@+id/cover_image"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_media_cover_art"
            android:clipToOutline="true"
            android:contentDescription="@string/media_cover_desc"
            android:scaleType="centerCrop"
            tools:src="@android:drawable/ic_media_play" />

        <!-- 3. Название трека -->
        <TextView
            android:id="@+id/track_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="@color/oem_cluster_title"
            android:textSize="14sp"
            tools:text="Запуск нейросетей..." />

        <!-- 4. Исполнитель -->
        <TextView
            android:id="@+id/track_artist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="@color/oem_cluster_subtitle"
            android:textSize="12sp"
            tools:text="Этот Компьютер" />

        <!-- 5. Progress: позиция | ======= | длительность -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/playback_position"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:minWidth="24dp"
                android:textColor="@color/oem_cluster_title"
                android:textSize="12sp"
                tools:text="0:00" />

            <androidx.appcompat.widget.AppCompatSeekBar
                android:id="@+id/playback_progress"
                android:layout_width="0dp"
                android:layout_height="4dp"
                android:layout_marginStart="6dp"
                android:layout_marginEnd="6dp"
                android:layout_weight="1"
                android:max="1000"
                android:progress="0"
                android:progressDrawable="@drawable/seekbar_cluster_progress"
                android:splitTrack="false"
                android:thumb="@null" />

            <TextView
                android:id="@+id/playback_duration"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:minWidth="24dp"
                android:textColor="@color/oem_cluster_title"
                android:textSize="12sp"
                tools:text="3:45" />

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
